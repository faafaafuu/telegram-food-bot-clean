# Настройка админ-панели

## Доступ к админ-панели

Админ-панель доступна по адресу: **https://mandanator.ru/webapp/admin.html**

## Способы авторизации

### 1. Через Telegram (рекомендуется)

**Через бота:**
1. Отправьте боту команду `/admin`
2. Нажмите кнопку "🔐 Открыть админ-панель"
3. Автоматический вход, если ваш ID в списке админов

**Прямой доступ:**
1. Откройте https://mandanator.ru/webapp/admin.html в Telegram встроенном браузере
2. Нажмите "Войти через Telegram"
3. Автоматическая авторизация

### 2. Через логин и пароль

**Для доступа из любого браузера:**
1. Откройте https://mandanator.ru/webapp/admin.html
2. Нажмите "🔑 Логин и пароль"
3. Введите учетные данные

**Настройка логина/пароля:**

Отредактируйте файл `.env`:

```env
ADMIN_USERNAME=admin          # ваш логин
ADMIN_PASSWORD=SecurePass123! # ваш пароль
```

⚠️ **Важно:** Используйте сложный пароль!

Примеры надежных паролей:
- `MyStr0ng!Pass#2025`
- `Admin_P@ssw0rd_2025!`
- `Secure#Food$Bot123`

## Добавление администраторов

### Telegram ID

Добавьте Telegram ID в файл `.env`:

```env
ADMIN_IDS=219450393,123456789,987654321
```

**Как узнать свой Telegram ID:**
1. Напишите боту @userinfobot
2. Скопируйте ваш ID
3. Добавьте в ADMIN_IDS через запятую

### Несколько администраторов

```env
ADMIN_IDS=219450393,555666777,888999000
ADMIN_USERNAME=admin
ADMIN_PASSWORD=SecurePass123!
```

- Каждый администратор может войти через Telegram (по своему ID)
- Все администраторы могут войти через общий логин/пароль

## Безопасность

### Защита от взлома

✅ **Автоматическая защита:**
- Rate limiting: максимум 5 попыток за 5 минут
- Блокировка IP при превышении лимита
- Токены с временем жизни 24 часа
- Шифрованные соединения (HTTPS)

### Рекомендации:

1. **Используйте сильный пароль**
   - Минимум 12 символов
   - Буквы, цифры, спецсимволы
   - Не используйте "admin", "123456", и т.д.

2. **Регулярно меняйте пароль**
   ```bash
   nano /root/telegram-food-bot-clean/.env
   # Измените ADMIN_PASSWORD
   docker-compose -f docker/docker-compose.prod.yml restart web
   ```

3. **Не делитесь паролем**
   - Каждому админу - свой Telegram ID
   - Пароль - только для экстренных случаев

4. **Проверяйте логи**
   ```bash
   docker logs docker-web-1 | grep "admin/auth"
   ```

## Применение изменений

После изменения `.env`:

```bash
cd /root/telegram-food-bot-clean/docker
docker-compose -f docker-compose.prod.yml restart web bot
```

## Восстановление доступа

### Забыли пароль?

1. Подключитесь к серверу по SSH
2. Отредактируйте `.env`:
   ```bash
   nano /root/telegram-food-bot-clean/.env
   ```
3. Измените `ADMIN_PASSWORD`
4. Перезапустите:
   ```bash
   cd /root/telegram-food-bot-clean/docker
   docker-compose -f docker-compose.prod.yml restart web
   ```

### Потеряли доступ к Telegram?

Используйте вход по логину/паролю через браузер.

### Полная блокировка?

Очистите блокировки IP (требуется перезапуск контейнера):
```bash
docker-compose -f docker/docker-compose.prod.yml restart web
```

## Примеры использования

### Сценарий 1: Владелец бизнеса
- Использует Telegram ID для быстрого доступа через бота
- Вход через `/admin` в @jafood_bot

### Сценарий 2: Менеджер
- Получил логин/пароль от владельца
- Заходит через браузер на компьютере
- https://mandanator.ru/webapp/admin.html → Логин/пароль

### Сценарий 3: Несколько администраторов
```env
ADMIN_IDS=111111111,222222222,333333333
ADMIN_USERNAME=jafood_admin
ADMIN_PASSWORD=SuperSecret#2025!
```
- Каждый может войти через свой Telegram
- Все могут войти через общий пароль

## Часто задаваемые вопросы

**Q: Можно ли отключить вход через пароль?**  
A: Да, удалите `ADMIN_USERNAME` и `ADMIN_PASSWORD` из `.env`

**Q: Токен истекает при каждом выходе?**  
A: Нет, токен живет 24 часа. Можно закрыть вкладку и вернуться.

**Q: Можно ли разные пароли для разных админов?**  
A: Сейчас один пароль для всех. Для разделения используйте Telegram ID.

**Q: Как посмотреть кто заходил?**  
A: Проверьте логи:
```bash
docker logs docker-web-1 --tail 100 | grep auth
```
